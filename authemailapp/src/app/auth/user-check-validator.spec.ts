import { UserCheckValidator } from './user-check-validator';

describe('UserCheckValidator', () => {
  it('should create an instance', () => {
    expect(new UserCheckValidator()).toBeTruthy();
  });
});
