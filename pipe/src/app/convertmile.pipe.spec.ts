import { ConvertmilePipe } from './convertmile.pipe';

describe('ConvertmilePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertmilePipe();
    expect(pipe).toBeTruthy();
  });
});
